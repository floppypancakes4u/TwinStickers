export declare const MathHelper: {
    DegToRad(degrees: number): number;
    RadToDeg(radians: number): number;
    Angle: {
        Wrap(angle: number): number;
    };
    Clamp(value: number, min: number, max: number): number;
};
